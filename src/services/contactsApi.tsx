import { createApi, fetchBaseQuery } from '@reduxjs/toolkit/query/react'
import { IContact } from '../models/contact.model'

type Props = {}

const contactsApi = (props: Props) => {
	return <div>contactsApi</div>
}

export default contactsApi
